#include <parser/parse.h>

ParserP** ParseLexerToCodegenBcode(size_t* ParserLen, lexer_t** lexer_code, size_t lexer_len)
{
  *ParserLen = 0;

  for (size_t i = 0; i < lexer_len; ++i)
  {

  }
  return NULL;
}
